FROM mysql:latest

ARG MYSQL_ROOT_PASSWORD
ARG MYSQL_USER
ARG MYSQL_PASSWORD

ENV MYSQL_ROOT_PASSWORD=${DB_ROOT_PASSWORD}
ENV MYSQL_DATABASE=dev
ENV MYSQL_USER=${DB_ADMIN_USERNAME}
ENV MYSQL_PASSWORD=${DB_ADMIN_PASSWORD}
ENV TZ=Asia/Tokyo

USER mysql
CMD ["mysqld"]